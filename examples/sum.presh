-- $ stack exec presheaf-lang-demo -- run 6 -- "$(cat examples/sum.presh)"
-- 18
let rec (sum : List Int -> Int) xs =
  case xs of
    { [] -> 0
    ; x :: xs -> x + (sum xs) }
in
\x. sum (x :: x :: x :: nil)
